import { DirectiveTutDirective } from './directive-tut.directive';

describe('DirectiveTutDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveTutDirective();
    expect(directive).toBeTruthy();
  });
});
